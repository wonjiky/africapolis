import {Sankey as type} from "d3plus-network";
import Shell from "./Viz";

/**
    @class Sankey
    @extends Viz
*/
var Sankey = (function (Shell) {
    function Sankey () {
        Shell.apply(this, arguments);
    }if ( Shell ) Sankey.__proto__ = Shell;
    Sankey.prototype = Object.create( Shell && Shell.prototype );
    Sankey.prototype.constructor = Sankey;

    

    return Sankey;
}(Shell));
Sankey.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default Sankey;

//# sourceMappingURL=Sankey.js.map