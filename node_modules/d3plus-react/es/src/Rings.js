import {Rings as type} from "d3plus-network";
import Shell from "./Viz";

/**
    @class Rings
    @extends Viz
*/
var Rings = (function (Shell) {
    function Rings () {
        Shell.apply(this, arguments);
    }if ( Shell ) Rings.__proto__ = Shell;
    Rings.prototype = Object.create( Shell && Shell.prototype );
    Rings.prototype.constructor = Rings;

    

    return Rings;
}(Shell));
Rings.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default Rings;

//# sourceMappingURL=Rings.js.map