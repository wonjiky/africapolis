import {Tree as type} from "d3plus-hierarchy";
import Shell from "./Viz";

/**
    @class Tree
    @extends Viz
*/
var Tree = (function (Shell) {
    function Tree () {
        Shell.apply(this, arguments);
    }if ( Shell ) Tree.__proto__ = Shell;
    Tree.prototype = Object.create( Shell && Shell.prototype );
    Tree.prototype.constructor = Tree;

    

    return Tree;
}(Shell));
Tree.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default Tree;

//# sourceMappingURL=Tree.js.map