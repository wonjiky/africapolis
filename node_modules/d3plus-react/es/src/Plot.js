import {Plot as type} from "d3plus-plot";
import Shell from "./Viz";

/**
    @class Plot
    @extends Viz
*/
var Plot = (function (Shell) {
    function Plot () {
        Shell.apply(this, arguments);
    }if ( Shell ) Plot.__proto__ = Shell;
    Plot.prototype = Object.create( Shell && Shell.prototype );
    Plot.prototype.constructor = Plot;

    

    return Plot;
}(Shell));
Plot.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default Plot;

//# sourceMappingURL=Plot.js.map