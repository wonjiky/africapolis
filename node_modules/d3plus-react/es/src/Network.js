import {Network as type} from "d3plus-network";
import Shell from "./Viz";

/**
    @class Network
    @extends Viz
*/
var Network = (function (Shell) {
    function Network () {
        Shell.apply(this, arguments);
    }if ( Shell ) Network.__proto__ = Shell;
    Network.prototype = Object.create( Shell && Shell.prototype );
    Network.prototype.constructor = Network;

    

    return Network;
}(Shell));
Network.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default Network;

//# sourceMappingURL=Network.js.map