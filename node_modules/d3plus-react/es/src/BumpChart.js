import {BumpChart as type} from "d3plus-plot";
import Shell from "./Viz";

/**
    @class BumpChart
    @extends Viz
*/
var BumpChart = (function (Shell) {
    function BumpChart () {
        Shell.apply(this, arguments);
    }if ( Shell ) BumpChart.__proto__ = Shell;
    BumpChart.prototype = Object.create( Shell && Shell.prototype );
    BumpChart.prototype.constructor = BumpChart;

    

    return BumpChart;
}(Shell));
BumpChart.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default BumpChart;

//# sourceMappingURL=BumpChart.js.map