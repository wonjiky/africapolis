import {AreaPlot as type} from "d3plus-plot";
import Shell from "./Viz";

/**
    @class AreaPlot
    @extends Viz
*/
var AreaPlot = (function (Shell) {
    function AreaPlot () {
        Shell.apply(this, arguments);
    }if ( Shell ) AreaPlot.__proto__ = Shell;
    AreaPlot.prototype = Object.create( Shell && Shell.prototype );
    AreaPlot.prototype.constructor = AreaPlot;

    

    return AreaPlot;
}(Shell));
AreaPlot.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default AreaPlot;

//# sourceMappingURL=AreaPlot.js.map