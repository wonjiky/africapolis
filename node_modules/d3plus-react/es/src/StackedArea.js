import {StackedArea as type} from "d3plus-plot";
import Shell from "./Viz";

/**
    @class StackedArea
    @extends Viz
*/
var StackedArea = (function (Shell) {
    function StackedArea () {
        Shell.apply(this, arguments);
    }if ( Shell ) StackedArea.__proto__ = Shell;
    StackedArea.prototype = Object.create( Shell && Shell.prototype );
    StackedArea.prototype.constructor = StackedArea;

    

    return StackedArea;
}(Shell));
StackedArea.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default StackedArea;

//# sourceMappingURL=StackedArea.js.map