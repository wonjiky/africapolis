import {LinePlot as type} from "d3plus-plot";
import Shell from "./Viz";

/**
    @class LinePlot
    @extends Viz
*/
var LinePlot = (function (Shell) {
    function LinePlot () {
        Shell.apply(this, arguments);
    }if ( Shell ) LinePlot.__proto__ = Shell;
    LinePlot.prototype = Object.create( Shell && Shell.prototype );
    LinePlot.prototype.constructor = LinePlot;

    

    return LinePlot;
}(Shell));
LinePlot.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default LinePlot;

//# sourceMappingURL=LinePlot.js.map