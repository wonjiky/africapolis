import {Geomap as type} from "d3plus-geomap";
import Shell from "./Viz";

/**
    @class Geomap
    @extends Viz
*/
var Geomap = (function (Shell) {
    function Geomap () {
        Shell.apply(this, arguments);
    }if ( Shell ) Geomap.__proto__ = Shell;
    Geomap.prototype = Object.create( Shell && Shell.prototype );
    Geomap.prototype.constructor = Geomap;

    

    return Geomap;
}(Shell));
Geomap.defaultProps = Object.assign({}, Shell.defaultProps, {type: type});
export default Geomap;

//# sourceMappingURL=Geomap.js.map